{% extends 'base.html.twig' %}

{% block body %}

    {% if(app.user.id != user.id) %}
        <div class="alert alert-danger" role="alert">{{ 'operation.danger' | trans }}</div>
    {% endif %}

    {% if(app.user.id == user.id) %}
        <h2>{{ user.username | capitalize }}</h2>

        <div class="row">
            <div class="col-xs-4">
                {{ dump(user) }}
            </div>
        </div>
    {% endif %}
{% endblock %}