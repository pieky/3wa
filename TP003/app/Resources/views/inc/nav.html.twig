<nav class="navbar navbar-default navbar-static-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ url('app.default.index') }}">TP003</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Training <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li><a href="{{ url('app.training.index') }}">Liste des Formations</a></li>
                        <li role="separator" class="divider"></li>
                        {% for key, training in trainings %}
                        <li><a href="{{ url('app.training.view', {'slug' : training.slug }) }}">{{ training.name }}</a></li>
                        {% endfor %}
                    </ul>
                </li>

                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Module <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li><a href="{{ url('app.module.index') }}">Liste des Modules</a></li>
                        <li role="separator" class="divider"></li>
                        {% for key, module in modules %}
                            <li><a href="{{ url('app.module.view', {'slug' : module.slug }) }}">{{ module.name }}</a></li>
                        {% endfor %}
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>