{% extends 'base.html.twig' %}

{% block body %}

    <div class="row subcategory-home">
        <h2>{{ subcategory.translations[currentLocal].name | capitalize }}</h2>

        {% for product in products %}
            {%  include 'product/one_product.html.twig' %}
        {% endfor %}
    </div>

{% endblock %}